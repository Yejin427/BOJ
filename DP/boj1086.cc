#include<iostream>
#include<vector>
#include<algorithm>
#include<string>
using namespace std;
vector<string> v;
int main() {
    int n, k; cin >> n;
    for(int i=1;i<=n;i++) {
        string num; cin >> num;
        v.push_back(num);
    }
    cin >> k;
    
}