#include<iostream>
#include<vector>
#include<queue>
using namespace std;
int arr[1000001];
vector<pair<int,int>> v;

int main(){
    int n; cin >> n;
    for(int i=1;i<=n;i++){
        int a,b; cin >> a >> b;
        v.push_back({a,b});
    }
    int l,p; cin >> l >> p;
    
    for(int i=0;i<v.size();i++){
        if()
    }
}